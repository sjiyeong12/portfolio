npm install -g nodemon
npm install pm2
//↑처음에 설치 했으면 안 해도 됨

npm install --save path
npm install express
npm install morgan // 로그
npm install body-parser // post

//비동기적인 것을 순차적으로 할 때
npm install fxjs
npm install ramda

gitgub desktop 사용